import 'package:flutter/widgets.dart';

class JithuText extends StatelessWidget {
  const JithuText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("jithu");
  }
}